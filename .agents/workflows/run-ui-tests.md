---
description: Run UI Component Tests (React & Vue)
---

To run the UI component tests for `get-lazy-var` specifically for React and Vue:

// turbo-all
1. Use vitest to run the react and vue tests
```bash
bun run vitest run tests/react.test.tsx tests/vue.test.ts
```
